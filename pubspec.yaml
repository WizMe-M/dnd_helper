name: dnd_helper
description: Helper for Game Mastering
publish_to: 'none'

version: 0.0.1+1

environment:
  sdk: '>=3.0.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  shouldly: ^0.5.0+1
  mockito: ^5.4.2
  isar: ^3.1.0+1
  isar_flutter_libs: ^3.1.0+1
  path: ^1.8.3
  path_provider: ^2.0.15
  mobx: ^2.2.0
  flutter_mobx: ^2.0.6+5
  get_it: ^7.6.0
  injectable: ^2.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  build_runner: ^2.4.5
  isar_generator: ^3.1.0+1
  mobx_codegen: ^2.3.0
  injectable_generator: ^2.1.6

flutter:
  uses-material-design: true
